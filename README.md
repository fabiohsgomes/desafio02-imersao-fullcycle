## Desafio Fullcycle Nest

Para subir os container da api nest e do banco mondodb, executar o comando abaixo:
``` bash
docker compose -f ./.docker/docker-compose.yml up
```
Ao executar o comando será baixada a imagem fabiohsgomes/desafio02-nest.

O arquivo api.http encontra-se na raiz do projeto.
